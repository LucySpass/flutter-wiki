<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET permission required for all HTTP calls (Wikipedia + Open-Meteo APIs).
         Flutter debug builds inject this automatically, but release/profile builds
         do NOT — the app must declare it explicitly here or the OS blocks all
         network access with "Failed host lookup" SocketException. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Location permissions required by the geolocator plugin.
         Without these, the "Use My Location" button will crash on Android.
         ACCESS_FINE_LOCATION = GPS-level precision.
         ACCESS_COARSE_LOCATION = network/cell-tower level (fallback). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:label="Fluwiki"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Package visibility queries — required since Android 11 (API 30).
         Apps must explicitly declare which intents they want to query, or
         canLaunchUrl() / queryIntentActivities() returns nothing. See:
         https://developer.android.com/training/package-visibility -->
    <queries>
        <!-- PROCESS_TEXT — used by the Flutter engine's ProcessTextPlugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- HTTPS VIEW — required by url_launcher's canLaunchUrl() so it can
             see the user's installed browsers. Without this, canLaunchUrl()
             returns false on Android 11+ even when launchUrl() would succeed. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
